/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you were using any specific Vite aliases that aren't standard, check tsconfig.
  // Standard @/* alias is supported by Next.js by default if in tsconfig.
};

export default nextConfig;
